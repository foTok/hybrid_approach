nn0~nn4 are trained with small data because it is not very important.

All the FE/DIA/HDIA models ended with "0" is trained in single fault data with plenty of data.
All the FE/DIA/HDIA models ended with "1" is trained in single fault data with small data.
